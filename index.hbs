{{!< default}}


{{#foreach posts}}
    <div class="post-preview">
    
        <h2 class="post-title"><a href="{{url}}">{{{title}}}</a></h2>
        <div class="post-meta">{{date published_at timeago="true"}} by {{author}}</div>
        <!--<div class="post-excerpt">{{excerpt}}</div>-->
    
    </div>
{{/foreach}}

    {{pagination}}

